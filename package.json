{
  "name": "vibma",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node dist/mcp.js",
    "socket": "tsx packages/tunnel/src/index.ts",
    "setup": "./scripts/setup.sh",
    "build": "npm run build -w packages/core && npm run build -w packages/adapter-figma && npm run build -w packages/tunnel",
    "build:watch": "npm run build:watch -w packages/core & npm run build:watch -w packages/adapter-figma",
    "dev": "node scripts/dev-mcp.mjs",
    "dev:mcp": "tsx packages/core/src/mcp.ts",
    "dev:watch": "npm run build:watch",
    "release": "./scripts/release.sh",
    "codegen": "tsx schema/compiler/index.ts",
    "docs:generate": "tsx schema/compiler/index.ts",
    "docs:dev": "npm run docs:generate && cd docs && npm run dev",
    "docs:build": "npm run docs:generate && cd docs && npm run build"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "pexels": "1.4.0",
    "untildify": "6.0.0",
    "yaml": "^2.8.2"
  }
}
